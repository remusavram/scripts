// This script creates a block of cubes

int $size = 10;

for ( $i=0; $i<$size; $i++ )
{
    for ( $j=0; $j<$size; $j++ )
    {
        for ( $k=0; $k<$size; $k++ )
        {
            polyCube;
            move ($i*2) ($j*2) ($k*2);
        }
    }
}